# 3D迷路ゲーム AGENTS.md（Three.js前提）

## 概要
本プロジェクトは、React + TypeScript + Three.js で構成された一人称視点の迷路ゲームです。  
迷路はランダム生成され、プレイヤーはチェックポイントを回収してからゴールへ到達することでクリアします。

## 実装前提（固定）

### 1. 描画方式
- 3D描画は **Three.js を唯一の正式実装** とする。
- CSS/Canvas による疑似3D実装は採用しない。
- 壁・床・柱・マーカーは Three.js のメッシュとして描画する。

### 2. 技術スタック
- UI: React
- 言語: TypeScript
- ビルド/開発サーバー: Vite
- 3D: Three.js（PerspectiveCamera）

### 3. 設計方針
- 仕様値は `src/game/constants.ts` に集約する。
- 画面制御とゲーム進行はフックへ集約し、描画ロジックと分離する。
- 3Dワールド構築処理は `src/components/maze3d/` 配下に集約する。

## 現行ゲーム仕様

### 1. 迷路と進行
- 迷路生成は深さ優先探索（DFS）で行う。
- スタートは左上、ゴールは右下を基準とする。
- チェックポイントを全回収するとゴールが有効化される。
- ゴール未解放で進入した場合は警告表示のみでクリアしない。

### 2. 操作とカメラ
- 操作キーは `↑`（前進）、`←`（左回転）、`→`（右回転）。
- カメラ移動は duration 指定で補間する。
- カメラ補間中は矢印入力をロックする。
- カメラは壁高さの半分を視点高さとし、地面と平行（ピッチ 0）を基準とする。

### 3. 描画仕様
- 壁は不透明で、背面が透けない構成を維持する。
- 壁交点には柱オブジェクトを配置する。
- 柱は角柱とし、壁厚ベースでわずかに太く設定する。
- 隣接壁がない交点にも柱を置く。
- スタート/チェックポイント/ゴールは床・壁の色で識別する。
- 壁色の強調は「そのセルに面している側のみ」を対象とする。

### 4. ヘルプ表示
- `H` でヘルプマップの表示を切り替える。
- ヘルプマップは3Dビュー上にオーバーレイ表示する。
- `D` でデバッグ用の未訪問領域開示を切り替える（ヘルプ表示中のみ）。

## 実装ルール

### 1. 定数管理
- 新しいゲーム仕様値・描画パラメータは `constants.ts` へ追加する。
- 直値の重複は避け、既存定数を優先利用する。
- 使われていない定数・設定は削除する。

### 2. 可読性と責務分離
- 処理が肥大化したファイルは分割し、責務を明確にする。
- まとまりのある処理はクラス/オブジェクトへカプセル化してよい。
- 共有ロジックは純関数化し、副作用を局所化する。

### 3. コメント規約（必須）
- **関数・メソッド・主要定数には日本語コメントを付与する。**
- 関数/メソッドコメントには **各引数の意味** を明記する。
- 主要定数には **値の意図と変更時影響** を短く書く。
- 複雑な条件分岐には「なぜ必要か」を1行で追記する。
- 既存コード修正時も、関連コメントを現仕様に合わせて更新する。

### 4. デバッグと品質
- デバッグログはフラグ定数で制御し、常時出力しない。
- 変更時は最低限 `npm run build` の成功を確認する。
- 挙動変更を伴う修正では、影響範囲（入力、表示、進行）を確認する。
